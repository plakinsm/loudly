import React from 'react';
import styles from './player.module.css'

export class PlayerСmp extends React.Component {
    render() {
        // const { currentSong } = this.props;
        // const iconUrl = currentSong ? currentSong.album.iconUrl : defaultAlbumCoverUrl;
        // const name = currentSong && currentSong.name;
        // const artist = currentSong && currentSong.artist;
        // const album = currentSong && currentSong.album;
        return (
            <div className={styles.container}>
            </div>
        )
    }
}